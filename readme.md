This is just a homework, so I'll be uploading binaries, fell free to ignore.

Win32: Compile under root with:
clang code/littlesnake.c code/main.c  -Icode -Idependencies/freeglut/include -Ldependencies/freeglut -o littlesnake.exe

